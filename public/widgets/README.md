# Standalone DAKboard Widgets

- `weather.html` — Bonney Lake weather/AQI
- `aviation.html` — KPLU METAR + KTCM TAF

Both pages load shared visual tokens from `../shared/dashboard-theme.css` but otherwise remain standalone HTML/JS pages for easy iframe embedding.
